# PX8-PN-D4 synthetic content conformance

A second lawful synthetic consumer used an opaque document lookup query, a
result containing one synthetic digest key, and the exact same `run_contact`
core. Producer→mule→consumer preserved the QueryRecord and ResultRecord
byte-for-byte and required no contact-core branch.

The test does not claim content retrieval, encryption, evidence, or import. It
exists only to disprove accidental coupling between D4 and FARO semantics.

```text
CONTENT_CONFORMANCE: PASS
APPLICATION_SPECIFIC_CONTACT_CORE_BRANCHES: 0
```
